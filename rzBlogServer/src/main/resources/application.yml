# Server Configuration
server:
  port: 8081
  servlet:
    context-path: "/api/"

# Active Profile Configuration
spring:
  profiles:
    active: test

mybatis:
  config-location: classpath:/mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.lhs.rzBlog.bean
